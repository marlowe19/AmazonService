version: '2'

services:
  aws.web:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

  amazon.api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"
